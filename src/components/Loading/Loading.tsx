import { StyledLoadingContainer, StyledSpinner } from "./Loading.style";

const LoadingSpinner = () => {
  return (
    <StyledLoadingContainer>
      <StyledSpinner />
    </StyledLoadingContainer>
  );
};

export default LoadingSpinner;
